# Knowledge Base — Global Pattern Library

Accumulated attack patterns, payload seeds, and technique digests from every target and session.

> Placeholder — initialized to resolve wiki index link. Populate with patterns as sessions complete.

---

## Patterns

_(Append one digest per finding. Format: technique → class → phase → payload seed → result.)_

---

## Payload Seeds

_(Seeds referenced by mcp/payload_mutator.py; keep base payloads here, never mutated variants.)_
